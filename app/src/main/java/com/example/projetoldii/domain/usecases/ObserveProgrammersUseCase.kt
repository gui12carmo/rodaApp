package com.example.projetoldii.domain.usecases

import com.example.projetoldii.repository.AddProgrammerRepository
import com.example.projetoldii.repository.ProgrammerItem
import kotlinx.coroutines.flow.Flow

class ObserveProgrammersUseCase(
    private val repo : AddProgrammerRepository
) {
//    operator fun invoke(projectId: Int): Flow<List<ProgrammerItem>> =
//        repo.observeProgrammers(projectId)
    }